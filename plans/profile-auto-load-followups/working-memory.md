# Working Memory — profile-auto-load-followups

## Active Gotchas

- **[Task 1] Atomicity.** Phase 2 Step 1 bullet AND line 99 template must be in ONE commit. Not two.
- **[Task 2] Prescribed exact strings.** Don't rewrite in active voice creatively — use the exact old→new strings in the PRD. Each agent file has a slightly different Layer 4 line; don't standardize their phrasing beyond what's prescribed.
- **[Task 3] Line 38 may have drifted.** Use anchor-string grep (`Loads scope-matched PROFILE.md bodies and any LEARNINGS.md`), not line 38 literally.

## Discovered Patterns
- [Task 2] active-framing pattern: each role file uses its own trigger-point verb (orchestrator=dispatch, developer=edit, reviewer=review, product-manager=shape) so the auto_load call reads as a natural first step within that role's workflow

## Context Corrections
*(To be added if any)*
